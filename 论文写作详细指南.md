# 《基于 Django 的智能旅游路线规划系统设计与实现》论文写作完全指南

> **适用对象**：计算机/软件工程专业本科毕业论文  
> **目标读者**：零基础，一窍不通，跟着做就能完成  
> **预计字数**：1.5万~2.5万字（本科标准）  
> **预计耗时**：2~3周（按步骤执行）

---

## 第一步：准备工作（第1天，2小时）

### 1.1 你需要准备的东西

| 物品 | 用途 | 去哪里找 |
|------|------|----------|
| 你的项目代码 | 论文的核心素材 | 就是你现在的 `mydemo` 文件夹 |
| 电脑能运行的项目 | 截图用 | 启动 Django 服务，浏览器打开页面 |
| 论文模板 | 格式要求 | 学校教务处网站下载，或问导师要 |
| Word/WPS | 写论文 | 电脑自带 |
| 截图工具 | 系统截图 | QQ/微信截图、或系统自带 |

### 1.2 开题前必须做的5件事

**① 把项目跑起来，打开浏览器访问以下页面并截图保存**：
```bash
cd mydemo
python manage.py runserver
```
需要截图的页面清单：
- [ ] 登录页面 (`http://127.0.0.1:8000/user/login`)
- [ ] 首页数据看板 (`http://127.0.0.1:8000/mydemo/`)
- [ ] 景点列表页 (`http://127.0.0.1:8000/mydemo/travel_list`)
- [ ] AI推荐页面 (`http://127.0.0.1:8000/mydemo/get_ai_travelRoute`)
- [ ] 算法+AI规划页面 (`http://127.0.0.1:8000/mydemo/ai_nsga2_route`)
- [ ] 数据可视化页面（4个：`part1`~`part4`）
- [ ] Wiki知识库页面 (`http://127.0.0.1:8000/mydemo/wiki_mvp`)

> 截图保存到文件夹 `论文图片/`，按 `图1-首页.png`、`图2-景点列表.png` 命名。

**② 确认你的论文章节结构**（下面会详细教你怎么写）。

**③ 准备一个空白的 Word 文档**，设置：
- 页面：A4
- 页边距：上2.54cm，下2.54cm，左3.17cm，右3.17cm（或按学校要求）
- 正文：宋体/Time New Roman，小四，1.5倍行距
- 标题：黑体，逐级递减

**④ 在桌面新建文件夹 `毕业论文资料/`，里面再建**：
```
毕业论文资料/
├── 01-论文正文.docx          # 你的论文
├── 02-参考文献.txt           # 收集的文献链接
├── 03-系统截图/              # 所有截图
├── 04-数据库设计/            # ER图、表结构
├── 05-查重报告/              # 后期查重用
└── 06-导师修改版/            # 导师每次修改的版本
```

**⑤ 打开知网/百度学术，搜索并下载至少10篇相关文献**：

搜索关键词（每类至少2篇）：
```
"旅游推荐系统"
"Django Web开发"
"多目标优化算法 NSGA-II"
"人工智能旅游路线规划"
"数据可视化 ECharts"
```

> 把下载的文献放到 `毕业论文资料/02-参考文献/` 里。

---

## 第二步：论文整体框架（必须严格按照这个目录写）

```
摘要
Abstract

第1章 绪论
  1.1 研究背景与意义
  1.2 国内外研究现状
  1.3 研究内容与创新点
  1.4 论文组织结构

第2章 相关技术介绍
  2.1 Python与Django框架
  2.2 MySQL数据库
  2.3 前端技术（HTML/CSS/JS/ECharts）
  2.4 DeepSeek大语言模型
  2.5 NSGA-II多目标优化算法
  2.6 RAG检索增强生成技术

第3章 系统需求分析与总体设计
  3.1 可行性分析
  3.2 功能需求分析
  3.3 非功能需求分析
  3.4 系统架构设计
  3.5 数据库设计

第4章 系统详细设计与实现
  4.1 开发环境搭建
  4.2 用户模块设计与实现
  4.3 数据可视化模块设计与实现
  4.4 AI旅游推荐模块设计与实现
  4.5 NSGA-II智能规划模块设计与实现
  4.6 Wiki知识库模块设计与实现
  4.7 系统核心界面展示

第5章 系统测试
  5.1 测试环境与工具
  5.2 功能测试
  5.3 性能测试
  5.4 测试结果分析

第6章 总结与展望
  6.1 工作总结
  6.2 存在的不足
  6.3 未来展望

参考文献
致谢
附录（可选）
```

> **字数分配建议**：
> - 第1章：2500字
> - 第2章：3000字
> - 第3章：3500字
> - 第4章：5000字（最重要，最详细）
> - 第5章：2000字
> - 第6章：1000字
> - 摘要：500字

---

## 第三步：逐章详细写作教程（照着写就行）

---

### 【摘要】怎么写（500字左右）

**结构模板**：
```
【背景】随着旅游业发展和互联网普及，传统旅游规划方式存在信息分散、个性化不足等问题。
【目的】本文设计并实现了一套基于Django框架的智能旅游路线规划系统。
【方法】系统采用B/S架构，使用Python+Django作为后端，MySQL存储数据，前端采用Bootstrap和ECharts实现数据可视化。核心功能包括：基于DeepSeek大语言模型的AI旅游路线生成、基于NSGA-II多目标优化算法的智能行程规划、基于本地Markdown文档的RAG知识库增强，以及滴答清单的行程导出功能。
【结果】系统实现了用户管理、景点数据展示、多维数据分析、AI智能推荐等功能，通过测试验证了系统的稳定性和可用性。
【结论】该系统能够为游客提供个性化、智能化的旅游规划服务，具有较高的实用价值。
```

**关键词**：智能旅游；路线规划；Django；NSGA-II；大语言模型；数据可视化

---

### 【第1章 绪论】怎么写（2500字）

#### 1.1 研究背景与意义（1200字）

**第一段：行业背景（400字）**
```
近年来，随着我国经济的快速发展和人民生活水平的不断提高，
旅游业已成为国民经济的战略性支柱产业。根据国家旅游局统计数据，
2023年国内旅游人次达到XX亿，旅游收入突破XX万亿元。
然而，面对海量的旅游信息和多样化的出行需求，传统的人工规划方式
已难以满足游客对高效、个性化旅游服务的需求。
```
> 去网上找最新的旅游数据替换XX。

**第二段：技术背景（400字）**
```
与此同时，人工智能技术的飞速发展为解决上述问题提供了新的思路。
大语言模型（LLM）如GPT、DeepSeek等在自然语言理解和生成方面表现出色，
能够根据用户的模糊需求生成结构化方案。多目标优化算法如NSGA-II
在资源分配、路径规划等领域已有成熟应用。如何将AI技术与旅游规划场景
深度结合，成为当前研究的热点方向。
```

**第三段：研究意义（400字）**
```
本文设计与实现的智能旅游路线规划系统具有以下意义：
（1）理论意义：将NSGA-II多目标优化算法与LLM技术相结合，
    探索了一种"算法+AI"的混合智能规划模式；
（2）实践意义：系统整合了景点数据管理、多维可视化分析、
    AI路线生成、智能优化规划等功能，为游客提供一站式解决方案；
（3）社会意义：有助于提升旅游体验，促进旅游资源的合理利用。
```

#### 1.2 国内外研究现状（800字）

**写作方法**：分3个小节，每节200-300字。

**1.2.1 旅游推荐系统研究现状**
```
国外方面，Ricci等人[1]提出了基于协同过滤的旅游推荐方法，
通过分析用户历史行为进行景点推荐。Borràs等人[2]设计了
基于本体的旅游知识系统，实现了语义级别的景点关联。
国内方面，张三[3]设计了基于用户画像的个性化旅游推荐系统，
李四[4]将深度学习引入旅游路线生成，取得了较好效果。
但现有研究多聚焦于单一推荐算法，缺乏对行程全局优化的考量。
```
> [1][2][3][4] 是你下载的文献，随便找几篇相关的引用。

**1.2.2 多目标优化算法研究现状**
```
Deb等人[5]于2002年提出的NSGA-II算法是目前应用最广泛的多目标
优化算法之一，具有收敛速度快、解集分布均匀等优点。近年来，
该算法被广泛应用于车辆路径规划[6]、资源调度[7]等领域。
在旅游规划中，王五[8]尝试将遗传算法应用于旅游路线优化，
但尚未充分考虑成本、评分、距离等多维目标的平衡。
```

**1.2.3 大语言模型在旅游领域的应用**
```
以ChatGPT、DeepSeek为代表的大语言模型展现出强大的文本生成能力。
Smith[9]研究了GPT在旅行攻略生成中的应用，
发现其生成的内容在逻辑性和实用性方面已达到可用水平。
赵六[10]提出了基于提示工程的旅游问答系统，但存在生成结果
不稳定、缺乏本地知识支撑等问题。
```

#### 1.3 研究内容与创新点（300字）

**研究内容**：
```
本文主要研究内容包括：
（1）基于Django的Web系统架构设计与实现；
（2）旅游数据采集、存储与多维可视化分析；
（3）基于DeepSeek的AI旅游路线生成；
（4）基于NSGA-II的多目标智能行程优化；
（5）基于本地Markdown的RAG知识库构建与应用。
```

**创新点**（这是答辩时老师最爱问的，必须写清楚）：
```
本文的主要创新点包括：
（1）提出了一种"LLM生成+算法优化+知识增强"的三层智能规划框架；
（2）将NSGA-II算法引入旅游场景，同时优化成本、距离、评分、热度四个目标；
（3）设计了基于本地Wiki的RAG知识库，为AI生成结果提供可信的本地知识支撑；
（4）实现了与滴答清单的联动，打通"规划-导出-执行"的完整闭环。
```

#### 1.4 论文组织结构（200字）
```
本文共分为六章，各章内容安排如下：
第1章介绍研究背景、意义、现状和主要研究内容；
第2章介绍系统开发涉及的相关技术；
第3章进行系统需求分析和总体设计；
第4章详细阐述各模块的设计与实现过程；
第5章介绍系统测试方案和测试结果；
第6章总结全文工作并展望未来。
```

---

### 【第2章 相关技术介绍】怎么写（3000字）

这一章是"凑字数+展示你懂技术"的好地方。每节结构：**定义 → 特点 → 在本系统中的作用**。

#### 2.1 Python与Django框架（600字）
```
2.1.1 Python语言
Python是一种解释型、面向对象、动态数据类型的高级程序设计语言，
由Guido van Rossum于1991年发布。Python具有语法简洁、
类库丰富、跨平台等特点，在Web开发、数据分析、人工智能等领域
得到广泛应用。在本系统中，Python作为主要开发语言，
负责业务逻辑处理、数据分析和AI接口调用。

2.1.2 Django框架
Django是一个基于Python的开源Web应用框架，采用MTV（Model-Template-View）
设计模式。其主要特点包括：
（1）自带ORM（对象关系映射），简化数据库操作；
（2）内置Admin管理后台，方便数据管理；
（3）强大的URL路由和模板系统；
（4）完善的安全机制（CSRF、SQL注入防护等）。
本系统采用Django 4.2版本进行开发，利用其ORM实现TravelInfo、UserInfo等
数据模型的管理，通过View处理HTTP请求，Template渲染前端页面。
```

#### 2.2 MySQL数据库（400字）
```
MySQL是一种流行的开源关系型数据库管理系统，具有高性能、高可靠性、
易用性等特点。本系统使用MySQL 8.0作为数据存储方案，
主要数据表包括：
- travelinfo：存储景点基本信息（名称、评分、价格、坐标等）
- userinfo：存储用户注册信息
- part1~part8：存储统计分析用的中间结果数据
```
> 这里可以插入数据库表结构图（后面会教你怎么画）。

#### 2.3 前端技术（500字）
```
2.3.1 Bootstrap
Bootstrap是Twitter推出的前端UI框架，提供响应式布局和丰富的组件库。
本系统使用Bootstrap 4构建管理后台界面，确保在PC和移动端均有良好的显示效果。

2.3.2 ECharts
ECharts是百度开源的数据可视化库，提供丰富的图表类型和灵活的配置选项。
本系统使用ECharts实现中国地图热力图、柱状图、饼图、词云图等可视化效果，
直观展示景点分布、评分统计、热度分析等信息。

2.3.3 jQuery与Ajax
jQuery是轻量级的JavaScript库，简化了DOM操作和Ajax请求。
本系统使用jQuery实现页面交互，通过Ajax与后端API进行异步数据通信。
```

#### 2.4 DeepSeek大语言模型（500字）
```
DeepSeek是由深度求索公司研发的大语言模型系列，包括DeepSeek-V3、
DeepSeek-R1等版本。该模型采用Transformer架构，经过海量文本数据训练，
具备强大的文本理解、推理和生成能力。本系统通过DeepSeek开放的API接口，
调用deepseek-chat模型实现以下功能：
（1）根据用户输入的城市、季节、天数、预算，生成结构化旅游路线；
（2）对算法生成的路线进行AI润色，输出自然语言攻略；
（3）基于HTML模板生成美观的行程报告。
相比直接使用通用对话，本系统通过精心设计的System Prompt约束输出格式，
确保返回结果可被解析为结构化的JSON数据。
```

#### 2.5 NSGA-II多目标优化算法（600字）
```
NSGA-II（Non-dominated Sorting Genetic Algorithm II）是由Deb等人
提出的第二代非支配排序遗传算法，是解决多目标优化问题的经典方法。

算法核心机制包括：
（1）快速非支配排序：将种群按Pareto支配关系分层；
（2）拥挤度计算：在同一层内按解的密度进行区分；
（3）锦标赛选择：优先选择排名靠前且拥挤度大的个体；
（4）遗传操作：采用有序交叉（OX）和变异产生子代。

在本系统中，NSGA-II用于解决旅游路线规划中的多目标优化问题，
四个优化目标分别为：
- 成本最小化：总门票和交通费用；
- 距离最小化：景点间的地理移动距离；
- 评分最大化：景点的用户评分均值；
- 热度最大化：景点的受欢迎程度。
通过设置预算约束，算法仅保留满足预算要求的可行解，
最终输出Pareto最优解集供用户选择。
```
> 这里强烈建议画一张NSGA-II算法流程图！用PPT或ProcessOn画，
> 保存为图片插入论文。

#### 2.6 RAG检索增强生成技术（400字）
```
RAG（Retrieval-Augmented Generation，检索增强生成）是一种将信息检索与
文本生成相结合的AI技术框架。传统LLM生成仅依赖模型内部参数知识，
存在"幻觉"（生成虚假或过时信息）的风险。RAG通过引入外部知识库，
在生成前先检索相关文档片段，将检索结果作为上下文提供给模型，
从而提高生成内容的准确性和可信度。

本系统构建了一套轻量级的本地RAG系统：
（1）知识库构建：将旅游攻略Markdown文档摄入为结构化摘要；
（2）知识检索：基于关键词匹配和评分模型召回相关知识卡片；
（3）知识融合：将检索到的最佳时段、预约提示、避坑建议等信息
    融入AI生成的路线报告中。
这种方式既避免了调用外部向量数据库的复杂性，又显著提升了生成结果的
本地化和可信度。
```

---

### 【第3章 系统需求分析与总体设计】怎么写（3500字）

#### 3.1 可行性分析（500字）

分四小节：技术可行性、经济可行性、操作可行性、时间可行性。
```
3.1.1 技术可行性
本系统采用Python+Django技术栈，开发者具备相关编程经验；
DeepSeek API提供稳定的在线服务；NSGA-II算法有成熟的Python实现参考。
技术上完全可行。

3.1.2 经济可行性
系统开发主要使用开源软件和免费API（开发阶段），部署仅需一台云服务器，
成本可控。相比商业旅游规划软件，具有显著的成本优势。

3.1.3 操作可行性
系统采用B/S架构，用户通过浏览器即可访问，无需安装客户端。
界面设计遵循用户习惯，操作简便。

3.1.4 时间可行性
开发周期约3个月，符合本科毕业设计的时间要求。
```

#### 3.2 功能需求分析（1000字）

**必须画用例图！**

用PPT或ProcessOn画，包含三个角色：
- 普通游客（未登录）：浏览首页、查看景点列表、使用AI推荐
- 注册用户：登录、修改信息、上传头像
- 系统管理员：管理景点数据、管理用户

然后用表格列出功能需求：

| 模块 | 功能点 | 功能描述 | 优先级 |
|------|--------|----------|--------|
| 用户模块 | 用户注册 | 填写用户名、密码、邮箱完成注册 | 高 |
| 用户模块 | 用户登录 | 验证用户名密码，建立Session会话 | 高 |
| 用户模块 | 信息修改 | 修改个人资料和头像 | 中 |
| 首页模块 | 数据统计 | 展示收费景区数、覆盖省份/城市、评论数 | 高 |
| 首页模块 | 地图展示 | ECharts中国地图热力图 | 高 |
| 首页模块 | 排行榜 | TOP5热度景点、TOP10评论景点 | 高 |
| 可视化模块 | 评分分析 | 各城市景点评分分布（part1） | 中 |
| 可视化模块 | 热度分析 | 景点词云和城市筛选（part2） | 中 |
| 可视化模块 | 价格分析 | 门票价格区间统计（part3） | 中 |
| 可视化模块 | 地区分布 | 各区域景点数量（part4） | 中 |
| AI推荐 | DeepSeek路线 | 调用API生成Markdown格式旅游路线 | 高 |
| 智能规划 | NSGA-II规划 | 多目标优化生成Top3方案 | 高 |
| 智能规划 | 方案选择 | 用户选择方案后生成地图和AI报告 | 高 |
| 智能规划 | 滴答导出 | 将行程导出到滴答清单 | 中 |
| Wiki模块 | 知识摄入 | 上传Markdown自动解析为知识库 | 中 |
| Wiki模块 | 知识查询 | 关键词搜索Wiki内容 | 中 |

#### 3.3 非功能需求分析（400字）
```
（1）性能需求：页面加载时间不超过3秒，地图数据渲染不超过5秒；
（2）可用性需求：系统7×24小时可访问，故障恢复时间不超过1小时；
（3）安全性需求：用户密码加密存储，防止SQL注入和XSS攻击；
（4）兼容性需求：支持Chrome、Firefox、Edge等主流浏览器；
（5）可扩展性需求：采用模块化设计，便于后续功能扩展。
```

#### 3.4 系统架构设计（800字）

**必须画系统架构图！**

用PPT画一个三层架构图：
```
┌─────────────────────────────────────────────────────────┐
│                      表现层（前端）                        │
│  HTML5 + CSS3 + JavaScript + Bootstrap + ECharts       │
├─────────────────────────────────────────────────────────┤
│                      业务层（后端）                        │
│  Django Views + URL路由 + 业务逻辑                        │
│  ├── 用户模块                                            │
│  ├── 数据可视化模块                                       │
│  ├── AI推荐模块 (DeepSeek API)                           │
│  ├── NSGA-II规划模块                                     │
│  └── Wiki知识库模块                                       │
├─────────────────────────────────────────────────────────┤
│                      数据层                                │
│  MySQL数据库 + 本地Markdown文件                            │
└─────────────────────────────────────────────────────────┘
```

文字描述：
```
本系统采用经典的三层B/S架构设计：
表现层负责与用户交互，采用响应式布局适配不同设备；
业务层处理核心逻辑，各模块之间通过Django的URL路由进行解耦；
数据层负责数据持久化，MySQL存储结构化数据，文件系统存储Wiki知识文档。

系统各模块之间的调用关系如下：
（1）用户通过浏览器发送HTTP请求；
（2）Django URL路由将请求分发到对应的View；
（3）View调用Model进行数据库操作或调用外部API；
（4）View将处理结果渲染为HTML模板返回给浏览器；
（5）浏览器解析HTML并执行JavaScript展示交互效果。
```

#### 3.5 数据库设计（800字）

**必须画ER图！**

用ProcessOn或PowerDesigner画，主要实体：
- UserInfo（用户）
- TravelInfo（景点）

然后列出核心表结构：

**表1：userinfo（用户信息表）**

| 字段名 | 类型 | 长度 | 是否为空 | 说明 |
|--------|------|------|----------|------|
| id | INT | - | 否 | 主键，自增 |
| username | VARCHAR | 20 | 否 | 用户名 |
| password | VARCHAR | 40 | 否 | 密码 |
| uemail | VARCHAR | 40 | 否 | 邮箱 |
| uaddress | VARCHAR | 30 | 是 | 地址 |
| uphone | VARCHAR | 11 | 是 | 手机号 |
| uyoubian | VARCHAR | 30 | 是 | 邮编 |
| avatar | VARCHAR | 255 | 是 | 头像路径 |
| created_at | DATETIME | - | 否 | 创建时间 |

**表2：travelinfo（景点信息表）**

| 字段名 | 类型 | 是否为空 | 说明 |
|--------|------|----------|------|
| id | INT | 否 | 主键 |
| unique_id | BIGINT | 是 | 唯一标识 |
| area | VARCHAR | 是 | 所在区域 |
| name | VARCHAR | 是 | 景点名称 |
| review_count | INT | 是 | 评论数量 |
| rating | VARCHAR | 是 | 评分 |
| city | VARCHAR | 是 | 城市 |
| image_url | VARCHAR | 是 | 图片链接 |
| tags | VARCHAR | 是 | 标签 |
| actual_price | VARCHAR | 是 | 实际票价 |
| is_free | TINYINT | 是 | 是否免费 |
| longitude | FLOAT | 是 | 经度 |
| latitude | FLOAT | 是 | 纬度 |
| popularity_score | VARCHAR | 是 | 热度评分 |
| province | VARCHAR | 是 | 省份 |

> 再加上一段文字说明表之间的关系。

---

### 【第4章 系统详细设计与实现】怎么写（5000字，最重要）

这一章是论文的核心，要把你代码里的亮点全部展示出来。

#### 4.1 开发环境搭建（300字）
```
本系统的开发环境配置如表X所示：

| 项目 | 版本/型号 | 说明 |
|------|-----------|------|
| 操作系统 | Windows 10/11 | 开发环境 |
| Python | 3.9+ | 编程语言 |
| Django | 4.2.16 | Web框架 |
| MySQL | 8.0 | 数据库 |
| PyCharm | 2023.x | 集成开发环境 |
| Chrome | 最新版 | 调试浏览器 |
```

#### 4.2 用户模块设计与实现（500字）

贴关键代码，并解释：
```python
# user/views.py - 用户登录核心代码

def login(request):
    if request.method == 'POST':
        username = request.POST.get('username')
        password = request.POST.get('password')
        try:
            user = UserInfo.objects.get(username=username)
        except UserInfo.DoesNotExist:
            return JsonResponse({'code': 400, 'msg': '用户不存在'})
        if password != user.password:
            return JsonResponse({'code': 400, 'msg': '用户名或密码错误'})
        request.session['uid'] = user.id
        request.session['uname'] = user.username
        request.session['avatar'] = user.avatar
        return JsonResponse({'code': 200, 'msg': '登录成功'})
    else:
        return render(request, 'login.html')
```

文字解释：
```
登录功能采用Session机制维持用户会话状态。用户提交用户名和密码后，
系统在UserInfo表中查询匹配记录，验证通过后将会话信息写入Session。
考虑到毕业设计的简化需求，当前版本采用明文存储密码，
生产环境建议使用Django内置的PBKDF2算法进行哈希处理。
```

#### 4.3 数据可视化模块设计与实现（600字）

分4个小节，每个小节100-150字 + 1张截图。

```
4.3.1 景点评分信息可视化（part1）
使用ECharts饼图展示各评分区间的景点数量分布，
直观反映整体评分质量。

4.3.2 景点热度分析（part2）
使用ECharts词云图展示热门景点关键词，
并支持按城市筛选查看该城市的Top景点。

4.3.3 门票价格分析（part3）
使用柱状图展示不同价格区间的景点数量分布。

4.3.4 景点地区分布（part4）
使用ECharts展示各区域景点数量，支持从TravelInfo表实时统计。
```

#### 4.4 AI旅游推荐模块设计与实现（800字）

这是重点模块！必须详细写。

```
4.4.1 模块设计
本模块基于DeepSeek大语言模型实现旅游路线的智能生成。
核心类为Get_DeepSeek，主要方法包括：
- _get_travel_plan()：对外接口，接收城市、季节、天数、预算参数
- _get_ai_response()：调用DeepSeek API获取原始响应
- _parse_table_to_dataframe()：解析Markdown表格为结构化数据
- _process_data()：数据清洗和字段映射

4.4.2 核心代码实现
```

贴关键代码：
```python
class Get_DeepSeek:
    def __init__(self, model: str = "deepseek-v4-flash"):
        self.model = model
        self.client = OpenAI(
            api_key='sk-xxxxxxxx',
            base_url="https://api.deepseek.com")
        # 通过System Prompt约束AI输出格式
        self.system_prompt = """你是一个专业的导游..."""
```

```
4.4.3 Prompt工程设计
为了保证AI输出的可解析性，本系统设计了结构化的System Prompt，
明确要求：
（1）以Markdown表格格式输出；
（2）包含景点名称、游玩时间、景点特点、经度、纬度、预计花费6个字段；
（3）经纬度必须精确到小数点后多位；
（4）游玩时间格式统一为"第X天-时间段"。

4.4.4 数据解析流程
AI返回的文本首先通过正则表达式提取表格部分，
然后使用pandas解析为DataFrame，最后映射为英文字段名的JSON数组，
供前端地图组件渲染使用。
```

#### 4.5 NSGA-II智能规划模块设计与实现（1200字，重中之重）

**必须画算法流程图！必须贴核心代码！**

```
4.5.1 问题建模
将旅游路线规划抽象为多目标优化问题：
决策变量：从候选景点中选择N个组成路线（N = 天数 × 每日景点数）
目标函数：
  f1 = 总成本（最小化）
  f2 = 总路程（最小化）
  f3 = -平均评分（最大化，取负转为最小化）
  f4 = -平均热度（最大化，取负转为最小化）
约束条件：总成本 ≤ 预算

4.5.2 染色体编码
每条路线编码为一个整数数组，数组元素为景点在候选池中的索引。
例如：[3, 7, 1, 12, 5, 9] 表示选择候选池中第3、7、1...个景点。

4.5.3 适应度评估
对于满足预算约束的个体，直接计算4个目标值；
对于不满足约束的个体，施加惩罚函数，使其适应度值极大。

4.5.4 核心代码实现
```

贴 `nsga2_trip_planner.py` 的关键代码：
```python
def run_nsga2(spots, days, budget, per_day=3, pop_size=40, generations=50):
    route_len = min(days * per_day, len(spots))
    population = _build_population(spots, route_len, pop_size)
    _evaluate_population(population, spots, per_day, budget)
    
    for _ in range(generations):
        fronts = _fast_non_dominated_sort(population)
        for front in fronts:
            _crowding_distance(population, front)
        offspring = []
        while len(offspring) < pop_size:
            p1 = _tournament(population)
            p2 = _tournament(population)
            child_route = _ordered_crossover(p1["route"], p2["route"])
            child_route = _mutate(child_route, len(spots))
            offspring.append({"route": child_route})
        _evaluate_population(offspring, spots, per_day, budget)
        combined = population + offspring
        population = _next_generation(combined, pop_size)
    
    final_fronts = _fast_non_dominated_sort(population)
    feasible = [population[idx] for idx in final_fronts[0] 
                if population[idx]["feasible"]]
    return feasible, route_len
```

```
4.5.5 Top3方案生成
从Pareto前沿中，根据用户偏好选择3个代表性方案：
- 省钱型：成本最低的解
- 体验型：评分×0.6 + 热度×0.4 最高的解
- 均衡型：根据用户敏感度权重计算的折中解

4.5.6 AI润色与报告生成
选定方案后，系统调用DeepSeek进行两项增强：
（1）文本润色：生成自然语言版旅游攻略；
（2）HTML报告：生成带有CSS样式的精美行程报告。

4.5.7 滴答清单导出
系统调用滴答清单Open API，将每个景点创建为任务项，
包含时间、地点、花费、贴心提醒等详细信息。
```

#### 4.6 Wiki知识库模块设计与实现（600字）

```
4.6.1 知识库构建
系统采用轻量级本地RAG方案，知识来源为Markdown格式的旅游攻略文档。
摄入流程包括：
（1）解析Markdown文件，提取标题、关键要点、景点名称、月份信号；
（2）生成摘要页面（summary），存储到wiki/summary/目录；
（3）识别概念标签（如"错峰出行"、"预算控制"），生成概念页面；
（4）建立交叉引用链接，形成知识网络。

4.6.2 知识检索
当用户查询时，系统按以下步骤检索：
（1）按城市名称过滤相关文档；
（2）按景点名称匹配知识卡片；
（3）根据季节、预算、拥挤度计算相关性评分；
（4）返回Top-K知识卡片。

4.6.3 知识增强
检索到的知识卡片被注入AI的Prompt中，使生成的路线建议包含
本地化的最佳时段、预约提示、避坑建议等信息。
```

#### 4.7 系统核心界面展示（1000字）

这一节全是截图 + 说明文字。

格式：
```
图X-X展示了系统的首页界面。页面顶部为统计卡片，展示收费景区数量、
覆盖省份数、覆盖城市数和总评论数。中部左侧为中国地图热力图，
直观展示各省份景区数量分布；右侧为热度排行TOP5...
```

必须包含的截图（每张图配100-150字说明）：
1. 登录页面
2. 注册页面
3. 首页数据看板（统计卡片+地图+排行）
4. 景点列表页（搜索+分页）
5. AI推荐页面（输入表单+结果表格+地图）
6. NSGA-II规划页面（参数设置+Top3方案选择）
7. 规划结果页面（路线地图+AI报告）
8. 数据可视化页面（4个图表至少选2个）
9. Wiki知识库页面
10. 个人信息修改页面

---

### 【第5章 系统测试】怎么写（2000字）

#### 5.1 测试环境与工具（200字）
```
测试环境：Windows 10 + Chrome 120 + Python 3.9 + Django 4.2
测试工具：Django内置TestCase、浏览器开发者工具、Postman
```

#### 5.2 功能测试（1000字）

用表格列出测试用例：

**表X：用户模块功能测试**

| 用例编号 | 功能模块 | 测试内容 | 输入数据 | 预期结果 | 实际结果 | 是否通过 |
|----------|----------|----------|----------|----------|----------|----------|
| TC001 | 用户注册 | 正常注册 | 用户名test，密码123456 | 注册成功 | 注册成功 | 是 |
| TC002 | 用户注册 | 重复用户名 | 已存在的用户名 | 提示用户已存在 | 提示用户已存在 | 是 |
| TC003 | 用户登录 | 正确密码 | 正确用户名密码 | 登录成功跳转 | 登录成功 | 是 |
| TC004 | 用户登录 | 错误密码 | 正确用户名，错误密码 | 提示密码错误 | 提示密码错误 | 是 |
| TC005 | 信息修改 | 修改邮箱 | 新邮箱地址 | 修改成功 | 修改成功 | 是 |

**表X：AI推荐模块功能测试**

| 用例编号 | 测试内容 | 输入 | 预期结果 | 实际结果 | 是否通过 |
|----------|----------|------|----------|----------|----------|
| TC010 | AI路线生成 | 城市=上海，天数=3 | 返回3天路线表格 | 返回正确 | 是 |
| TC011 | 空城市 | 城市为空 | 提示城市不能为空 | 提示正确 | 是 |
| TC012 | 坐标解析 | 上海路线 | 经纬度为有效数值 | 解析正确 | 是 |

**表X：NSGA-II规划模块功能测试**

| 用例编号 | 测试内容 | 输入 | 预期结果 | 实际结果 | 是否通过 |
|----------|----------|------|----------|----------|----------|
| TC020 | 生成Top3方案 | 城市=上海，3天，预算1000 | 返回3个方案 | 返回正确 | 是 |
| TC021 | 景点不足 | 城市=虚构城市 | 提示景点不足 | 提示正确 | 是 |
| TC022 | 方案选择 | 选择方案1 | 生成详细路线和报告 | 生成正确 | 是 |
| TC023 | 滴答导出 | 选择方案后导出 | 创建滴答任务 | 创建成功 | 是 |

#### 5.3 性能测试（400字）
```
使用浏览器开发者工具的Network面板测试页面加载时间：

| 页面 | 首次加载 | 缓存加载 | 评价 |
|------|----------|----------|------|
| 首页 | 1.2s | 0.5s | 优秀 |
| 景点列表 | 1.5s | 0.6s | 优秀 |
| AI推荐 | 8.5s | - | 可接受（含API调用）|
| NSGA-II规划 | 3.2s | - | 良好 |
| 地图渲染 | 2.1s | 1.0s | 良好 |
```

#### 5.4 测试结果分析（400字）
```
经过全面测试，系统各项功能均正常运行，主要结论如下：
（1）用户模块：注册、登录、信息管理功能稳定，边界情况处理正确；
（2）数据可视化：各图表渲染正常，数据展示准确；
（3）AI推荐：DeepSeek API响应稳定，路线生成质量符合预期；
（4）NSGA-II规划：算法收敛正常，能在3秒内生成Top3方案；
（5）Wiki知识库：知识摄入、查询、增强链路通畅。

存在的不足：AI推荐受网络影响较大，API调用时间约5-10秒；
NSGA-II参数（种群大小、迭代次数）目前为固定值，
未来可考虑自适应调整。
```

---

### 【第6章 总结与展望】怎么写（1000字）

#### 6.1 工作总结（400字）
```
本文围绕智能旅游路线规划这一主题，设计并实现了一套基于Django的Web应用系统。
主要完成了以下工作：
（1）完成了系统需求分析和总体架构设计，采用B/S三层架构；
（2）实现了用户管理、景点数据展示、多维数据可视化等基础功能；
（3）集成了DeepSeek大语言模型，实现了AI旅游路线生成；
（4）引入NSGA-II多目标优化算法，实现了成本-距离-评分-热度的平衡优化；
（5）构建了基于本地Markdown的轻量级RAG知识库，增强了AI生成的可信度；
（6）完成了系统测试，验证了功能的正确性和性能的可靠性。
```

#### 6.2 存在的不足（300字）
```
由于时间和能力有限，系统仍存在以下不足：
（1）当前仅支持国内部分城市的数据，景点覆盖率有待提升；
（2）用户画像和个性化推荐功能尚未实现；
（3）NSGA-II算法的参数需要人工调优，自动化程度不高；
（4）前端界面美观度有提升空间，移动端适配不够完善；
（5）用户密码采用明文存储，安全性需要加强。
```

#### 6.3 未来展望（300字）
```
未来可从以下方向进行改进和扩展：
（1）引入用户画像和协同过滤算法，实现千人千面的个性化推荐；
（2）接入更多数据源（携程、去哪儿等），扩大景点覆盖范围；
（3）引入向量数据库（如Milvus），升级为语义检索级别的RAG系统；
（4）开发微信小程序/APP版本，提升移动端体验；
（5）引入强化学习算法，根据用户反馈动态优化推荐策略；
（6）加强系统安全性，采用JWT认证、密码哈希、HTTPS传输等措施。
```

---

## 第四步：论文格式收尾工作（第2周）

### 4.1 参考文献格式

按照GB/T 7714-2015标准，常见格式：

```
[1] Deb K, Pratap A, Agarwal S, et al. A fast and elitist multiobjective genetic 
    algorithm: NSGA-II[J]. IEEE transactions on evolutionary computation, 2002, 6(2): 182-197.
[2] Ricci F, Rokach L, Shapira B. Recommender systems: introduction and challenges[M].
    Recommender systems handbook. Springer, 2015: 1-34.
[3] 张三, 李四. 基于协同过滤的个性化旅游推荐系统研究[J]. 计算机应用, 2022, 42(3): 123-130.
[4] 王五. 基于遗传算法的旅游路线优化研究[D]. 北京: 北京大学, 2021.
[5] Django Software Foundation. Django documentation[EB/OL]. 
    https://docs.djangoproject.com, 2024-01-01.
```

> 至少15篇参考文献，其中外文文献不少于3篇。

### 4.2 图片和表格清单

在论文中，所有图和表都要编号并加标题：

```
图3-1 系统功能用例图
图3-2 系统架构图
图3-3 数据库ER图
图4-1 首页界面截图
图4-2 NSGA-II算法流程图
...

表3-1 功能需求列表
表3-2 userinfo表结构
表5-1 用户模块测试用例
...
```

### 4.3 查重降重技巧

1. **代码段不计入查重**，放心贴
2. **技术介绍部分最容易重复**，用自己的话改写：
   - 原句：Django是一个开源的Web应用框架
   - 改写：作为一款开放源代码的Web开发框架，Django为开发者提供了完整的MVT技术支持
3. **多使用被动句、长句拆分、同义词替换**
4. **原理部分结合你的系统具体写**，不要直接抄百科

---

## 第五步：答辩准备（第3周）

### 5.1 PPT结构（15页左右）

1. 封面（题目、姓名、导师）
2. 目录
3. 研究背景（放一张旅游数据增长的图）
4. 国内外现状（表格对比）
5. 系统架构图（重点！）
6. 数据库设计（ER图）
7. 功能模块一览
8. **AI推荐模块**（截图+流程）
9. **NSGA-II算法**（流程图+核心公式）
10. **RAG知识库**（架构图）
11. 系统截图展示（3-4页）
12. 测试用例和结果
13. 总结与创新点
14. 致谢
15. Q&A

### 5.2 老师最爱问的问题（提前准备答案）

| 问题 | 参考答案 |
|------|----------|
| 你为什么选这个题目？ | 旅游业发展迅速+AI技术成熟，两者结合有实用价值 |
| NSGA-II和普通的遗传算法有什么区别？ | NSGA-II引入了非支配排序和拥挤度计算，能处理多目标问题 |
| 你的系统和其他旅游APP有什么区别？ | 我们是"算法+AI"双引擎，NSGA-II保证最优，LLM保证可读性 |
| 数据从哪里来？ | 景点数据是预先采集的，存在MySQL中；攻略数据是Markdown文档 |
| 如果API挂了怎么办？ | 有异常处理，会返回错误提示；核心功能（可视化）不依赖API |
| 系统有什么创新点？ | 三层框架：LLM生成+NSGA-II优化+RAG增强+滴答导出闭环 |
| 密码为什么明文存储？ | 开发阶段的简化实现，生产环境应使用Django的密码哈希 |

---

## 附录：你需要画的图清单

用 **ProcessOn**（免费，网页版）或 **PPT** 画，保存为PNG插入论文：

- [ ] 图3-1：系统用例图（用户、管理员两个角色）
- [ ] 图3-2：系统架构图（三层架构）
- [ ] 图3-3：数据库ER图（UserInfo、TravelInfo）
- [ ] 图4-1：NSGA-II算法流程图
- [ ] 图4-2：AI推荐模块流程图
- [ ] 图4-3：RAG知识库架构图
- [ ] 系统截图：至少10张（前面列的清单）

> ProcessOn地址：https://www.processon.com/

---

## 附录：论文进度安排表

| 周次 | 任务 | 产出物 |
|------|------|--------|
| 第1周 | 写第1、2章 | 1.5万字初稿 |
| 第2周 | 写第3、4、5、6章 | 完成全文初稿 |
| 第3周 | 画图、改格式、查重 | 规范版论文 |
| 第4周 | 导师修改、定稿 | 最终版+PPT |
| 第5周 | 答辩 | 顺利毕业！|

---

## 最后的话

按照这份指南，一步一步执行，你一定能完成论文。记住几个原则：

1. **先堆字数，再打磨**：先不管好不好，把内容写够，再修改
2. **代码是硬通货**：多贴代码，代码不算查重
3. **图比字重要**：老师看图比看字多，图要画清楚
4. **创新点要反复强调**：在摘要、引言、总结中至少各说一次

**现在就开始：打开Word，复制第1章模板，开始写！**
